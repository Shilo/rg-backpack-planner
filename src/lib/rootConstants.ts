/** Root node ID (index-based, like -1). Used for links, data-node-id, and excluding from level logic. */
export const ROOT_ID = "-1";

export const ROOT_X = 0;
export const ROOT_Y = 0;
export const ROOT_RADIUS = 60 / 65;
export const ROOT_SIZE = 64 * ROOT_RADIUS;
